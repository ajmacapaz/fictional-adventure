name: hello-world
on: 
  #push:
  workflow_dispatch: #manual_trigger_enabled
jobs:
  hello-world-job:
    runs-on: ubuntu-latest
    steps:
      - name: checkout repository code
        uses: actions/checkout@v4
      - run: echo "$(cat hello_world.txt)"
